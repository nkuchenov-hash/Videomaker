const $=(s,r=document)=>r.querySelector(s), $$=(s,r=document)=>[...r.querySelectorAll(s)];
const esc=(v='')=>String(v).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const now=new Date('2026-08-11T04:02:00-04:00');
const providers={
  youtube:{icon:'▶',label:'YouTube',color:'#ff0033'}, linkedin:{icon:'in',label:'LinkedIn',color:'#0a66c2'},
  tiktok:{icon:'♪',label:'TikTok',color:'#111111'}, instagram:{icon:'◎',label:'Instagram',color:'#c13584'},
  facebook:{icon:'f',label:'Facebook',color:'#1877f2'}, telegram:{icon:'➤',label:'Telegram',color:'#229ed9'},
  vk:{icon:'VK',label:'VK',color:'#0077ff'}, x:{icon:'X',label:'X',color:'#111111'}
};
const providerKeys=Object.keys(providers);
const themes=['IntraWeb 17 FE','Release notes','Tutorials','How-to / snippets','Devlog','Performance','Customer showcase','Community'];
const formats=['Post','Image post','Short','Reel','Long video'];
const nav=[['dashboard','⌂','Dashboard'],['content','▤','Content Library'],['analytics','⌁','Analytics'],['calendar','□','Calendar'],['publisher','↑','Publisher'],['knowledge','◇','Knowledge Base']];
const STORE='cms-iw-unified-preview-v3';
const iso=(offset,h=10,m=0)=>{const d=new Date(now);d.setDate(d.getDate()+offset);d.setHours(h,m,0,0);return d.toISOString()};
const uid=(p='id')=>`${p}-${Date.now().toString(36)}-${Math.random().toString(36).slice(2,7)}`;
const when=p=>p.publishedAt||p.scheduledAt||p.createdAt;
const dateLabel=v=>new Intl.DateTimeFormat('en-US',{month:'short',day:'numeric',year:'numeric'}).format(new Date(v));
const dayLabel=v=>new Intl.DateTimeFormat('en-US',{weekday:'short',month:'short',day:'numeric'}).format(new Date(v));
const timeLabel=v=>new Intl.DateTimeFormat('en-US',{hour:'numeric',minute:'2-digit'}).format(new Date(v));
const compactNum=v=>Intl.NumberFormat('en-US',{notation:Number(v)>=10000?'compact':'standard',maximumFractionDigits:1}).format(Number(v||0));
const providerBadge=p=>`<span class="provider ${p}">${providers[p].icon} ${providers[p].label}</span>`;
const statusBadge=s=>`<span class="status ${s}">${({review:'Needs review',scheduled:'Scheduled',draft:'Draft',published:'Published',failed:'Failed'})[s]||s}</span>`;
const head=(title,copy,meta='')=>`<div class="page-head"><div><h1>${esc(title)}</h1><p>${esc(copy)}</p></div>${meta?`<div class="meta">${meta}</div>`:''}</div>`;

const upcoming=[
 ['p01','IntraWeb 17 FE: what changed for existing projects','youtube','review','Long video','IntraWeb 17 FE','A practical walkthrough of the changes existing IntraWeb projects will notice first — project structure, iteration speed, compatibility notes and the safest migration path.',1,15,0,'08:24','landscape',3,'Final cut + thumbnail'],
 ['p02','Weekly build notes — faster iteration, clearer project structure','linkedin','review','Post','Release notes','This week in IntraWeb 17 FE: faster iteration, clearer project structure, and several fixes based directly on developer feedback.\n\nThe important part is how much less friction there is between edit, run and verify.',1,10,0,'—','text',4,'Text post'],
 ['p03','60 sec: make a responsive form behave on mobile','youtube','scheduled','Short','Tutorials','One small layout pattern that keeps the active field visible above the mobile keyboard — without hiding the primary action.',2,12,30,'00:58','vertical',2,'Vertical video'],
 ['p04','60 sec: responsive form layout','instagram','scheduled','Reel','Tutorials','A compact IntraWeb 17 pattern for responsive forms. Save this for the next time a mobile editor starts fighting the keyboard.',2,13,0,'00:58','vertical',2,'Vertical video'],
 ['p05','Responsive forms: the rule that prevents most mobile pain','tiktok','scheduled','Short','Tutorials','If the user cannot see the field they are typing into, the form is broken. Here is the layout rule we use to avoid that.',2,14,15,'00:46','vertical',1,'Vertical video'],
 ['p06','Rendering performance: before / after','linkedin','scheduled','Image post','Performance','A compact before/after breakdown for teams deciding when to move a production project to IntraWeb 17.',3,11,30,'—','square',2,'1 image + copy'],
 ['p07','Rendering performance — the practical numbers','facebook','review','Image post','Performance','What actually changes in a production project? This comparison focuses on practical numbers and trade-offs instead of headline benchmarks.',3,16,0,'—','square',1,'1 image + copy'],
 ['p08','Migration checklist: 7 things to verify first','x','scheduled','Post','How-to / snippets','Moving an existing project to IntraWeb 17? Start with these 7 checks before changing anything else. Thread ↓',4,9,15,'—','text',2,'Thread · 7 posts'],
 ['p09','Migration checklist — save this before upgrading','telegram','scheduled','Post','How-to / snippets','Before upgrading a production IntraWeb project, verify these seven things first. The checklist is short enough to keep next to the project while you work.',4,9,30,'—','text',2,'Text + link'],
 ['p10','Customer project: rebuilding an internal portal','youtube','draft','Long video','Customer showcase','A customer project walkthrough: what the team rebuilt, which parts of the old workflow they kept, and where IntraWeb 17 changed the implementation.',5,17,0,'11:12','landscape',1,'Rough cut'],
 ['p11','3 mistakes in component state management','tiktok','review','Short','Tutorials','Three state mistakes that look harmless in a small demo and become painful in a real project.',5,12,0,'00:49','vertical',3,'Vertical video'],
 ['p12','3 state mistakes — developer cut','instagram','scheduled','Reel','Tutorials','Three tiny state mistakes. Three very non-tiny consequences once the project grows.',5,12,20,'00:52','vertical',3,'Vertical video'],
 ['p13','Devlog: what we changed after user feedback','linkedin','review','Post','Devlog','Several of this week’s changes came directly from developer feedback. Here is what we changed, what we deliberately did not change, and why.',6,10,30,'—','text',2,'Text post'],
 ['p14','Release notes visual summary','facebook','scheduled','Image post','Release notes','This week’s changes in one visual: what moved, what was fixed and what developers should verify in existing projects.',6,14,0,'—','square',1,'Carousel · 4 slides'],
 ['p15','Release notes visual summary','vk','review','Image post','Release notes','Короткая визуальная сводка изменений недели: что изменилось, что исправлено и что стоит проверить в существующих проектах.',6,14,15,'—','square',1,'Carousel · 4 slides'],
 ['p16','Why this small rendering change matters','x','review','Post','Performance','A small rendering change can remove a surprising amount of friction in a large project. The important part is where it appears in the daily workflow.',7,8,45,'—','text',1,'Thread · 5 posts'],
 ['p17','Long-form tutorial: production-ready responsive pages','youtube','scheduled','Long video','Tutorials','Build a responsive page from the first component to the final production checks, with the common failure modes included.',8,16,0,'18:36','landscape',5,'Final video'],
 ['p18','Production-ready responsive pages — teaser','instagram','draft','Reel','Tutorials','The full tutorial is long. This is the 42-second version of the one idea you need before you start.',8,12,0,'00:42','vertical',1,'Rough vertical cut'],
 ['p19','Ask IntraWeb: what should we test next?','linkedin','scheduled','Post','Community','We are planning the next round of practical tests. Which workflow would be most useful to see measured end to end?',9,10,0,'—','text',1,'Text post'],
 ['p20','Ask IntraWeb: next tutorial topic','telegram','scheduled','Post','Community','What should the next practical tutorial cover? Send the workflow that costs you the most time right now.',9,10,15,'—','text',1,'Poll + text'],
 ['p21','Performance myth: more components always means slower UI','tiktok','review','Short','Performance','More components does not automatically mean a slower UI. The bottleneck is usually somewhere more specific.',10,13,0,'00:54','vertical',2,'Vertical video'],
 ['p25','IntraWeb 17 FE in 4 screenshots','vk','scheduled','Image post','IntraWeb 17 FE','Four screenshots that show the new workflow without a long explanation.',11,11,0,'—','square',1,'Carousel · 4 slides'],
 ['p26','One tiny UI rule that saves support time','facebook','review','Post','How-to / snippets','A tiny layout decision can prevent a surprising number of support questions. Here is the rule and where it matters.',11,15,30,'—','text',1,'Text post'],
 ['p27','What we are measuring next week','x','scheduled','Post','Devlog','Next week we are measuring three workflow changes end to end: edit-to-run time, mobile form friction, and rendering stability.',12,9,0,'—','text',1,'Thread · 4 posts']
].map(([id,title,provider,status,format,theme,copy,d,h,m,duration,aspect,revision,asset],i)=>({id,canonicalId:`c-${String(i+1).padStart(2,'0')}`,title,provider,status,format,theme,copy,scheduledAt:iso(d,h,m),duration,aspect,revision,asset,sourceUrl:`https://example.com/content/${id}`,createdAt:iso(d-5,9,0),views:0,reach:0,likes:0,comments:0,shares:0}));

const pastTitles=[
 ['IntraWeb 17 FE — first look','IntraWeb 17 FE'],['The simplest way to explain IntraWeb 17 FE','IntraWeb 17 FE'],['Quick tip: keep the primary action reachable','How-to / snippets'],['Release notes: week 31','Release notes'],['Why iteration speed matters more than benchmark headlines','Performance'],['Responsive layout checklist','Tutorials'],['Customer portal: what changed after migration','Customer showcase'],['Devlog: three fixes from support tickets','Devlog'],['Component state: the safe default','Tutorials'],['Migration tip: verify routes first','How-to / snippets'],['Release summary in 60 seconds','Release notes'],['Performance test: large component tree','Performance'],['Community question: what should we benchmark?','Community'],['IntraWeb 17 FE: compatibility notes','IntraWeb 17 FE'],['Mobile editor: keeping input visible','Tutorials'],['Customer showcase: internal admin tool','Customer showcase'],['Devlog: why we kept this behavior','Devlog'],['Three migration mistakes to avoid','How-to / snippets'],['Release notes: component updates','Release notes'],['Performance myth: render count vs perceived speed','Performance'],['How to structure a production page','Tutorials'],['Community results: requested topics','Community'],['IntraWeb 17 FE: deployment workflow','IntraWeb 17 FE'],['UI review: mobile forms','Tutorials']
];
const past=pastTitles.map(([title,theme],i)=>{
  const provider=providerKeys[i%providerKeys.length], days=-(i+2), views=Math.round((900+(i%8)*620)*(1+i*.055));
  return {id:`hist-${i+1}`,canonicalId:`hc-${i+1}`,title,provider,status:'published',format:i%5===0?'Long video':i%4===0?'Image post':i%3===0?'Short':'Post',theme,copy:`Published ${providers[provider].label} content about ${theme}. This record demonstrates linked publication history, provider metrics and cross-module filtering.`,publishedAt:iso(days,9+(i%7),i%2?30:0),duration:i%5===0?'07:42':i%3===0?'00:52':'—',aspect:i%5===0?'landscape':i%4===0?'square':i%3===0?'vertical':'text',revision:1+(i%4),asset:i%5===0?'Published video':i%4===0?'Published image':'Published post',sourceUrl:`https://example.com/published/${i+1}`,createdAt:iso(days-6,10,0),views,reach:Math.round(views*1.55),likes:Math.round(views*.048),comments:Math.round(views*.006),shares:Math.round(views*.009)};
});

const defaultKnowledge=[
 {id:'k1',kind:'Manual',title:'Responsive layout guidelines',theme:'Tutorials',source:'Team',items:18,delta:2,summary:'Rules for mobile forms, keyboard-safe editing and primary action placement.'},
 {id:'k2',kind:'Internal parse',title:'IntraWeb 17 FE release notes',theme:'Release notes',source:'Official IntraWeb',items:94,delta:8,summary:'Release changes parsed from official workspace and product sources.'},
 {id:'k3',kind:'External',title:'Delphi productivity competitor channel',theme:'Community',source:'Competitor A',items:56,delta:19,summary:'External publishing activity and topic clusters for competitor intelligence.'},
 {id:'k4',kind:'External',title:'Web framework news watch',theme:'Performance',source:'Industry news',items:105,delta:31,summary:'External news and technical discussions classified by workspace topics.'},
 {id:'k5',kind:'Internal parse',title:'Support questions cluster',theme:'How-to / snippets',source:'Support archive',items:71,delta:14,summary:'Repeated user questions converted into reusable content opportunities.'},
 {id:'k6',kind:'Manual',title:'Product terminology and forbidden wording',theme:'IntraWeb 17 FE',source:'Team',items:26,delta:0,summary:'Approved product wording, naming and editorial constraints.'}
];
const defaultHero={product:'IntraWeb 17 FE is a developer-focused evolution of the IntraWeb workflow. Content should explain practical changes and reduce friction for real projects.',audience:'Delphi and IntraWeb developers, technical teams evaluating upgrades, existing customers and developers looking for practical implementation guidance.',voice:'Technical, practical, concise, evidence-oriented. Prefer concrete workflows, examples and trade-offs over promotional language.',sources:['Official IntraWeb website','IntraWeb 17 FE documentation','Published social channels','Support questions','Customer examples','Competitor channels']};
const defaultConnections=Object.fromEntries(providerKeys.map(p=>[p,p==='youtube'||p==='linkedin'||p==='telegram']));
const defaultWidgets={pipeline:true,coverage:true,analytics:true,knowledge:true};

function loadModel(){
  const base={posts:[...past,...upcoming],knowledge:defaultKnowledge,hero:defaultHero,connections:defaultConnections,dashboardWidgets:defaultWidgets};
  try{const saved=JSON.parse(localStorage.getItem(STORE)||'null');if(saved){return {...base,...saved,hero:{...defaultHero,...saved.hero},connections:{...defaultConnections,...saved.connections},dashboardWidgets:{...defaultWidgets,...saved.dashboardWidgets}}}}catch{}
  return base;
}
let model=loadModel();
const save=()=>localStorage.setItem(STORE,JSON.stringify(model));

const state={route:location.hash.replace('#/','')||'dashboard',contentProvider:'all',contentTheme:'all',contentStatus:'all',contentSearch:'',analyticsMetric:'views',analyticsProviders:new Set(providerKeys),analyticsPeriod:30,analyticsTheme:'all',analyticsFormat:'all',calView:'month',calProviders:new Set(),calStatus:'all',calCursor:new Date(now),pubProvider:'all',pubStatus:'all',pubSearch:'',pubSelected:model.posts.find(p=>p.status==='review')?.id||model.posts[0]?.id,pubTab:'preview',knowledgeKind:'all',knowledgeTheme:'all',knowledgeSearch:''};

function renderShell(){
  document.body.innerHTML=`<div class="app"><aside class="rail"><div class="mark">IW</div><div id="railNav" class="rail-nav"></div><div class="spacer"></div><button class="rail-btn" data-route="settings" title="Settings">⚙</button></aside><aside class="sidebar"><div class="workspace-head"><div><div class="eyebrow">CONTENT SYSTEM</div><div class="workspace-name">IntraWeb 17</div></div><span class="dot"></span></div><button class="hero-link" data-route="hero"><span class="hero-icon">✦</span><span><b>Hero Setup</b><small>Workspace intelligence</small></span><span>›</span></button><div class="section-title">Workspace</div><nav id="sideNav" class="nav"></nav><button class="profile" data-route="settings"><span class="avatar">N</span><span><b>Profile</b><small>Workspace administrator</small></span><span>›</span></button></aside><main class="main"><header class="topbar"><div class="crumb">CMS IW <span>/</span> <b id="crumb"></b></div><div class="top-actions"><button class="btn" id="globalSearch">⌕ Search</button><button class="btn primary" id="newContent">＋ New content</button></div></header><section id="view" class="view"></section></main></div><dialog id="composer" class="composer"></dialog><div id="toast" class="toast" aria-live="polite"></div>`;
  renderNav(); bindRoutes(); $('#newContent').onclick=openComposer; $('#globalSearch').onclick=()=>{location.hash='#/content';setTimeout(()=>$('#contentSearch')?.focus(),30)};
}
function renderNav(){const labels={dashboard:'Dashboard',content:'Content Library',analytics:'Analytics',calendar:'Calendar',publisher:'Publisher',knowledge:'Knowledge Base',hero:'Hero Setup',settings:'Settings'};$('#crumb').textContent=labels[state.route]||'Dashboard';$('#railNav').innerHTML=nav.map(([r,i,l])=>`<button class="rail-btn ${state.route===r?'active':''}" data-route="${r}" title="${l}">${i}</button>`).join('');$('#sideNav').innerHTML=nav.map(([r,i,l])=>`<button class="${state.route===r?'active':''}" data-route="${r}"><span>${i}</span><span>${l}</span></button>`).join('');bindRoutes()}
function bindRoutes(){$$('[data-route]').forEach(b=>b.onclick=()=>{location.hash='#/'+b.dataset.route})}
function toast(msg){const t=$('#toast');t.textContent=msg;t.classList.add('show');clearTimeout(toast.timer);toast.timer=setTimeout(()=>t.classList.remove('show'),1900)}
function goPublisher(id){state.pubSelected=id;location.hash='#/publisher'}

function dashboard(){
  const pub=model.posts.filter(p=>p.status==='published'), sched=model.posts.filter(p=>p.status==='scheduled'), review=model.posts.filter(p=>p.status==='review');
  const cards=`<div class="cards"><div class="card"><span>Published</span><b class="value">${pub.length}</b><p>content history</p></div><div class="card"><span>Scheduled</span><b class="value">${sched.length}</b><p>upcoming queue</p></div><div class="card"><span>Needs review</span><b class="value">${review.length}</b><p>editorial attention</p></div><div class="card"><span>Total views</span><b class="value">${compactNum(pub.reduce((a,p)=>a+p.views,0))}</b><p>published content</p></div></div>`;
  const upcomingRows=model.posts.filter(p=>p.status!=='published').sort((a,b)=>new Date(when(a))-new Date(when(b))).slice(0,8).map(p=>`<button class="queue-line clickable" data-open-post="${p.id}"><div><b>${esc(p.title)}</b><p>${dayLabel(when(p))} · ${timeLabel(when(p))}</p></div><div class="meta">${providerBadge(p.provider)}${statusBadge(p.status)}</div></button>`).join('');
  const coverage=themes.slice(0,5).map(t=>{const c=pub.filter(p=>p.theme===t).length,v=pub.length?Math.round(c/pub.length*100):0;return `<div class="coverage"><div><b>${esc(t)}</b><span>${v}%</span></div><div class="track"><i style="width:${v}%"></i></div></div>`}).join('');
  const mini=renderMiniAnalytics();
  const knowledgeTotal=model.knowledge.reduce((a,k)=>a+k.items,0);
  return `${head('Dashboard','Configurable workspace overview assembled from the same data used by Publisher, Calendar, Analytics and Knowledge Base.','<span class="pill accent">Connected preview</span>')}${cards}<div class="dashboard-grid">${model.dashboardWidgets.pipeline?`<section class="panel span2"><div class="panel-title"><div><h2>Publishing pipeline</h2><p>Click an item to open the same publication in Publisher.</p></div><button class="btn" data-route="publisher">Open Publisher</button></div>${upcomingRows}</section>`:''}${model.dashboardWidgets.coverage?`<section class="panel dark"><h2>Theme coverage</h2><p>Share of published content by theme.</p>${coverage}</section>`:''}${model.dashboardWidgets.analytics?`<section class="panel span2"><div class="panel-title"><div><h2>Analytics pulse</h2><p>Views over the last 14 days across connected channels.</p></div><button class="btn" data-route="analytics">Full analytics</button></div>${mini}</section>`:''}${model.dashboardWidgets.knowledge?`<section class="panel"><h2>Knowledge growth</h2><b class="big-number">${knowledgeTotal}</b><p>knowledge items tracked</p><div class="queue-line"><span>External sources this week</span><b>+${model.knowledge.reduce((a,k)=>a+k.delta,0)}</b></div><button class="btn" data-route="knowledge">Open Knowledge Base</button></section>`:''}</div>`;
}

function contentLibrary(){
  const q=state.contentSearch.toLowerCase();const filtered=model.posts.filter(p=>(state.contentProvider==='all'||p.provider===state.contentProvider)&&(state.contentTheme==='all'||p.theme===state.contentTheme)&&(state.contentStatus==='all'||p.status===state.contentStatus)&&(!q||`${p.title} ${p.copy} ${p.theme} ${p.format}`.toLowerCase().includes(q))).sort((a,b)=>new Date(when(b))-new Date(when(a)));
  return `${head('Content Library','Complete content history and planned records. CMS IW stores links, metadata, previews and publication results — not the primary media binaries.','<span class="pill accent">'+filtered.length+' records</span>')}<div class="filters"><label class="wide"><span>Search</span><input id="contentSearch" value="${esc(state.contentSearch)}" placeholder="Title, theme, structure, campaign…"></label><label><span>Channel</span><select id="contentProvider"><option value="all">All channels</option>${providerKeys.map(p=>`<option value="${p}" ${state.contentProvider===p?'selected':''}>${providers[p].label}</option>`).join('')}</select></label><label><span>Theme</span><select id="contentTheme"><option value="all">All themes</option>${themes.map(t=>`<option ${state.contentTheme===t?'selected':''}>${t}</option>`).join('')}</select></label><label><span>Status</span><select id="contentStatus"><option value="all">All statuses</option>${['published','scheduled','review','draft'].map(s=>`<option value="${s}" ${state.contentStatus===s?'selected':''}>${s==='review'?'Needs review':s[0].toUpperCase()+s.slice(1)}</option>`).join('')}</select></label></div><div class="table"><div class="tr head"><span>Status</span><span>Content</span><span>Channel</span><span>Theme</span><span>Format</span><span>Date</span><span>Actions</span></div>${filtered.map(p=>`<div class="tr content-row"><span>${statusBadge(p.status)}</span><div><b>${esc(p.title)}</b><p>${esc(p.copy.slice(0,120))}${p.copy.length>120?'…':''}</p><a class="asset-link" href="${esc(p.sourceUrl)}" target="_blank" rel="noopener">↗ linked source</a></div><span>${providerBadge(p.provider)}</span><span>${esc(p.theme)}</span><span>${esc(p.format)}</span><span>${dateLabel(when(p))}</span><span><button class="btn small" data-open-post="${p.id}">Open in Publisher</button></span></div>`).join('')}</div>`;
}

const metrics={views:'Views',reach:'Reach',likes:'Likes',comments:'Comments',shares:'Shares',watch:'Watch time'};
function analyticsValue(provider,dayIndex,metric){
  const pi=providerKeys.indexOf(provider)+1, growth=.5+dayIndex/90, wave=1+Math.sin((dayIndex+pi*2.3)/5)*.16, base=(160+pi*105)*growth*wave*(1+pi*.045);
  const views=Math.max(8,Math.round(base));
  if(metric==='views')return views;
  if(metric==='reach')return Math.round(views*(1.35+pi*.025));
  if(metric==='likes')return Math.round(views*(.035+pi*.002));
  if(metric==='comments')return Math.round(views*(.0045+pi*.00035));
  if(metric==='shares')return Math.round(views*(.006+pi*.00045));
  if(metric==='watch')return Math.round(views*(provider==='youtube'?3.6:(provider==='tiktok'||provider==='instagram'?0.7:0.25)));
  return views;
}
function niceMax(v){if(v<=0)return 10;const pow=10**Math.floor(Math.log10(v)),n=v/pow;const nice=n<=1?1:n<=2?2:n<=2.5?2.5:n<=5?5:10;return nice*pow}
function analyticsSeries(){const days=Math.max(7,Number(state.analyticsPeriod)),start=new Date(now);start.setDate(start.getDate()-days+1);start.setHours(12,0,0,0);const labels=[];for(let i=0;i<days;i++){const d=new Date(start);d.setDate(start.getDate()+i);labels.push(d)}const active=[...state.analyticsProviders];const factor=(state.analyticsTheme==='all'?1:.58)*(state.analyticsFormat==='all'?1:.64);return {labels,lines:active.map(p=>({provider:p,color:providers[p].color,values:labels.map((_,i)=>Math.round(analyticsValue(p,90-days+i,state.analyticsMetric)*factor))}))}}
function lineChartMarkup(){
  const {labels,lines}=analyticsSeries();if(!lines.length)return '<div class="chart-empty">Select at least one channel.</div>';
  const W=1240,H=520,L=112,R=34,T=30,B=74,IW=W-L-R,IH=H-T-B,maxRaw=Math.max(...lines.flatMap(l=>l.values),1),maxY=niceMax(maxRaw*1.08),ticks=5;
  const yGrid=Array.from({length:ticks+1},(_,i)=>{const value=maxY*(ticks-i)/ticks,y=T+IH*i/ticks;return `<line class="a-grid" x1="${L}" x2="${W-R}" y1="${y}" y2="${y}"/><text class="a-y" x="${L-18}" y="${y+7}" text-anchor="end">${compactNum(value)}</text>`}).join('');
  const xEvery=Math.max(1,Math.ceil(labels.length/8));const xTicks=labels.map((d,i)=>{if(i%xEvery!==0&&i!==labels.length-1)return '';const x=L+(labels.length===1?0:IW*i/(labels.length-1));return `<text class="a-x" x="${x}" y="${H-24}" text-anchor="middle">${new Intl.DateTimeFormat('en-US',{month:'short',day:'numeric'}).format(d)}</text>`}).join('');
  const paths=lines.map(line=>{const pts=line.values.map((v,i)=>`${L+(labels.length===1?0:IW*i/(labels.length-1))},${T+IH-(v/maxY)*IH}`).join(' ');return `<polyline class="a-line" data-line="${line.provider}" points="${pts}" style="stroke:${line.color}"/>`}).join('');
  return `<div class="line-chart" data-chart><svg viewBox="0 0 ${W} ${H}" role="img" aria-label="${metrics[state.analyticsMetric]} by channel over time">${yGrid}${paths}${xTicks}<line class="a-hover-line" data-hover-line x1="0" x2="0" y1="${T}" y2="${T+IH}" visibility="hidden"/><rect class="a-hit" data-hit x="${L}" y="${T}" width="${IW}" height="${IH}"/><g data-points></g></svg><div class="chart-tooltip" data-tooltip hidden></div></div>`;
}
function analytics(){
  const published=model.posts.filter(p=>p.status==='published'), selected=[...state.analyticsProviders],{labels,lines}=analyticsSeries();const total=lines.reduce((a,l)=>a+l.values.reduce((x,y)=>x+y,0),0),peak=Math.max(0,...lines.flatMap(l=>l.values));
  const filteredPublished=published.filter(p=>selected.includes(p.provider)&&(state.analyticsTheme==='all'||p.theme===state.analyticsTheme)&&(state.analyticsFormat==='all'||p.format===state.analyticsFormat)&&new Date(when(p))>=new Date(now.getTime()-state.analyticsPeriod*86400000));
  const providerButtons=`<button data-a-provider="all" class="${selected.length===providerKeys.length?'active':''}">All</button>${providerKeys.map(p=>`<button data-a-provider="${p}" class="${state.analyticsProviders.has(p)?'active':''}"><i style="background:${providers[p].color}"></i>${providers[p].label}</button>`).join('')}`;
  return `${head('Analytics','One analytical screen for all channels. The graph, summary and breakdowns respond to the same filters.','<span class="pill accent">'+state.analyticsPeriod+' days</span>')}<div class="analytics-controls"><div class="filters compact-filters"><label><span>Period</span><select id="aPeriod">${[7,30,90].map(v=>`<option value="${v}" ${state.analyticsPeriod===v?'selected':''}>${v} days</option>`).join('')}</select></label><label><span>Theme</span><select id="aTheme"><option value="all">All themes</option>${themes.map(t=>`<option ${state.analyticsTheme===t?'selected':''}>${t}</option>`).join('')}</select></label><label><span>Format</span><select id="aFormat"><option value="all">All formats</option>${formats.map(f=>`<option ${state.analyticsFormat===f?'selected':''}>${f}</option>`).join('')}</select></label></div><div class="channel-multiselect"><b>Channels</b><div>${providerButtons}</div></div></div><div class="analytics-hero"><section class="chart-panel"><div class="chart-head"><div><span class="eyebrow">TREND</span><h2>${metrics[state.analyticsMetric]}</h2><p>Daily values. Y-axis auto-scales to selected channels and metric.</p></div><div class="legend">${lines.map(l=>`<span><i style="background:${l.color}"></i>${providers[l.provider].label}</span>`).join('')}</div></div><div class="metric-switch">${Object.entries(metrics).map(([k,l])=>`<button data-metric="${k}" class="${state.analyticsMetric===k?'active':''}">${l}</button>`).join('')}</div>${lineChartMarkup()}</section><aside class="summary-panel dark-summary"><span class="eyebrow">SELECTED METRIC</span><h2>${metrics[state.analyticsMetric]}</h2><p>Same channel/theme/format filters as the graph.</p><div class="summary-main"><span>Total</span><b>${compactNum(total)}</b></div><div class="summary-row"><span>Average / day</span><b>${compactNum(total/Math.max(1,labels.length))}</b></div><div class="summary-row"><span>Peak channel-day</span><b>${compactNum(peak)}</b></div><div class="summary-row"><span>Selected channels</span><b>${selected.length}</b></div><div class="summary-row"><span>Published posts</span><b>${filteredPublished.length}</b></div><div class="summary-row"><span>Y-axis maximum</span><b>${compactNum(niceMax(peak*1.08))}</b></div></aside></div><div class="cards analytics-kpis">${[['Views','views'],['Reach','reach'],['Likes','likes'],['Comments','comments'],['Shares','shares'],['Watch min','watch']].map(([label,key])=>{const old=state.analyticsMetric;const factor=(state.analyticsTheme==='all'?1:.58)*(state.analyticsFormat==='all'?1:.64);const value=selected.reduce((a,p)=>a+Array.from({length:state.analyticsPeriod},(_,i)=>analyticsValue(p,90-state.analyticsPeriod+i,key)).reduce((x,y)=>x+y,0)*factor,0);return `<div class="card"><span>${label}</span><b class="value medium">${compactNum(value)}</b></div>`}).join('')}</div>`;
}
function bindChart(){const wrap=$('[data-chart]');if(!wrap)return;const svg=$('svg',wrap),hit=$('[data-hit]',wrap),line=$('[data-hover-line]',wrap),tip=$('[data-tooltip]',wrap),points=$('[data-points]',wrap);const {labels,lines}=analyticsSeries();const W=1240,H=520,L=112,R=34,T=30,B=74,IW=W-L-R,IH=H-T-B,maxY=niceMax(Math.max(...lines.flatMap(l=>l.values),1)*1.08);function move(ev){const rect=svg.getBoundingClientRect(),x=(ev.clientX-rect.left)/rect.width*W,idx=Math.max(0,Math.min(labels.length-1,Math.round((x-L)/IW*(labels.length-1)))),cx=L+IW*idx/(labels.length-1);line.setAttribute('x1',cx);line.setAttribute('x2',cx);line.setAttribute('visibility','visible');points.innerHTML=lines.map(l=>{const cy=T+IH-(l.values[idx]/maxY)*IH;return `<circle cx="${cx}" cy="${cy}" r="6" fill="${l.color}" stroke="white" stroke-width="3"/>`}).join('');tip.hidden=false;tip.innerHTML=`<b>${new Intl.DateTimeFormat('en-US',{weekday:'short',month:'short',day:'numeric'}).format(labels[idx])}</b>${lines.map(l=>`<span><i style="background:${l.color}"></i>${providers[l.provider].label}<strong>${compactNum(l.values[idx])}</strong></span>`).join('')}`;const px=Math.min(rect.width-260,Math.max(12,ev.clientX-rect.left+14));tip.style.left=`${px}px`;tip.style.top=`${Math.max(10,ev.clientY-rect.top-60)}px`}hit.addEventListener('mousemove',move);hit.addEventListener('mouseleave',()=>{line.setAttribute('visibility','hidden');points.innerHTML='';tip.hidden=true})}
function renderMiniAnalytics(){const selected=['youtube','linkedin','telegram'],days=14,W=800,H=210,L=24,R=12,T=15,B=25,IW=W-L-R,IH=H-T-B,max=niceMax(Math.max(...selected.flatMap(p=>Array.from({length:days},(_,i)=>analyticsValue(p,76+i,'views'))))*1.08);return `<div class="mini-chart"><svg viewBox="0 0 ${W} ${H}">${selected.map(p=>{const pts=Array.from({length:days},(_,i)=>`${L+IW*i/(days-1)},${T+IH-analyticsValue(p,76+i,'views')/max*IH}`).join(' ');return `<polyline points="${pts}" style="stroke:${providers[p].color}"/>`}).join('')}</svg><div class="legend">${selected.map(p=>`<span><i style="background:${providers[p].color}"></i>${providers[p].label}</span>`).join('')}</div></div>`}

const weekdays=['Monday','Tuesday','Wednesday','Thursday','Friday','Saturday','Sunday'];
function monday(d){const x=new Date(d),off=(x.getDay()+6)%7;x.setDate(x.getDate()-off);x.setHours(0,0,0,0);return x}
function sameDay(a,b){return new Date(a).toDateString()===new Date(b).toDateString()}
function calendarEntries(){return model.posts.filter(p=>(!state.calProviders.size||state.calProviders.has(p.provider))&&(state.calStatus==='all'||p.status===state.calStatus)).sort((a,b)=>new Date(when(a))-new Date(when(b)))}
function calCard(p){return `<button class="cal-card ${p.provider} ${p.status}" data-open-post="${p.id}"><div class="meta">${providerBadge(p.provider)}${statusBadge(p.status)}</div><b>${esc(p.title)}</b><span class="compact">${timeLabel(when(p))} · ${esc(p.format)}</span></button>`}
function calendar(){
  const entries=calendarEntries(),cursor=state.calCursor;let body='';
  if(state.calView==='month'){const first=new Date(cursor.getFullYear(),cursor.getMonth(),1),start=monday(first),days=[];for(let i=0;i<42;i++){const d=new Date(start);d.setDate(start.getDate()+i);days.push(d)}body=`<div class="calendar-frame"><div class="weekdays">${weekdays.map(w=>`<div class="weekday">${w}</div>`).join('')}</div><div class="month-grid">${days.map(d=>{const rows=entries.filter(p=>sameDay(when(p),d));return `<section class="daycell ${d.getMonth()!==cursor.getMonth()?'muted-day':''}"><div class="dayhead"><b>${d.getDate()}</b>${rows.length>1?`<span class="count">${rows.length} posts</span>`:''}</div><div class="cal-stack">${rows.map(calCard).join('')}</div></section>`}).join('')}</div></div>`}
  else if(state.calView==='week'){const start=monday(cursor);body=`<div class="week-grid">${weekdays.map((w,i)=>{const d=new Date(start);d.setDate(start.getDate()+i);const rows=entries.filter(p=>sameDay(when(p),d));return `<section class="week-col"><h3>${w}</h3><p>${dateLabel(d)}</p><div class="cal-stack">${rows.length?rows.map(calCard).join(''):'<p class="empty-inline">No posts</p>'}</div></section>`}).join('')}</div>`}
  else{const rows=entries.filter(p=>sameDay(when(p),cursor));body=`<div class="day-view"><div class="day-view-head"><h2>${new Intl.DateTimeFormat('en-US',{weekday:'long',month:'long',day:'numeric'}).format(cursor)}</h2></div>${rows.length?rows.map(p=>`<div class="day-row"><b>${timeLabel(when(p))}</b>${calCard(p)}</div>`).join(''):'<div class="panel"><p>No posts for this day.</p></div>'}</div>`}
  const providerFilter=`<button data-cal-provider="all" class="${!state.calProviders.size?'active':''}">All</button>${providerKeys.map(p=>`<button data-cal-provider="${p}" class="${state.calProviders.has(p)?'active':''}">${providers[p].label}</button>`).join('')}`;
  const period=state.calView==='month'?new Intl.DateTimeFormat('en-US',{month:'long',year:'numeric'}).format(cursor):state.calView==='week'?`Week of ${dateLabel(monday(cursor))}`:dateLabel(cursor);
  return `${head('Content calendar','Published and upcoming content share one calendar. Provider filters are multi-select and every card opens the exact item in Publisher.','<span class="pill accent">'+esc(period)+'</span>')}<div class="calendar-toolbar"><div class="segmented">${['month','week','day'].map(v=>`<button data-cal-view="${v}" class="${state.calView===v?'active':''}">${v[0].toUpperCase()+v.slice(1)}</button>`).join('')}</div><div class="calendar-nav"><button class="btn" data-cal-move="-1">‹</button><button class="btn" data-cal-today>Today</button><button class="btn" data-cal-move="1">›</button></div></div><div class="calendar-filters"><div class="provider-filter">${providerFilter}</div><select id="calStatus"><option value="all">All statuses</option>${['published','scheduled','review','draft'].map(s=>`<option value="${s}" ${state.calStatus===s?'selected':''}>${s==='review'?'Needs review':s[0].toUpperCase()+s.slice(1)}</option>`).join('')}</select></div>${body}`;
}

function pubFiltered(){return model.posts.filter(p=>(state.pubProvider==='all'||p.provider===state.pubProvider)&&(state.pubStatus==='all'||p.status===state.pubStatus)&&(!state.pubSearch||`${p.title} ${p.theme} ${p.copy}`.toLowerCase().includes(state.pubSearch.toLowerCase()))).sort((a,b)=>new Date(when(a))-new Date(when(b)))}
function mediaPreview(p){if(p.aspect==='text')return '';const cls=p.aspect==='vertical'?'vertical':p.aspect==='square'?'square':'';return `<div class="media ${cls}"><div class="media-art"><span>${esc(p.theme)}</span><strong>${esc(p.title)}</strong><span>${esc(p.duration)}</span></div>${['Short','Reel','Long video'].includes(p.format)?'<button class="play" type="button">▶</button>':''}</div>`}
function publisher(){
  const list=pubFiltered();if(!list.find(p=>p.id===state.pubSelected)&&list[0])state.pubSelected=list[0].id;const sel=model.posts.find(p=>p.id===state.pubSelected)||list[0];const stats={review:model.posts.filter(p=>p.status==='review').length,scheduled:model.posts.filter(p=>p.status==='scheduled').length,draft:model.posts.filter(p=>p.status==='draft').length,published:model.posts.filter(p=>p.status==='published').length};
  const groups=new Map();list.forEach(p=>{const key=new Date(when(p)).toDateString();if(!groups.has(key))groups.set(key,[]);groups.get(key).push(p)});
  const queue=[...groups.entries()].map(([key,rows])=>`<div class="queue-group"><div class="queue-group-title"><b>${dayLabel(rows[0]&&when(rows[0]))}</b><span class="count">${rows.length}</span></div>${rows.map(p=>`<button class="queue-item ${p.id===sel?.id?'active':''}" data-select-post="${p.id}"><span class="thumb ${p.aspect==='vertical'?'vertical':''} ${p.aspect==='square'?'image':''}">${providers[p.provider].icon}</span><span class="queue-copy"><b>${esc(p.title)}</b><span class="compact">${timeLabel(when(p))} · ${esc(p.format)}</span><span class="meta">${providerBadge(p.provider)}${statusBadge(p.status)}</span></span></button>`).join('')}</div>`).join('');
  return `${head('Publisher','Interactive editorial workbench: queue, destination preview, copy, assets, schedule and actions stay visible together.','<span class="pill accent">'+model.posts.length+' publications</span>')}<div class="publisher-stats">${[['Needs review',stats.review],['Scheduled',stats.scheduled],['Drafts',stats.draft],['Published',stats.published],['Next 7 days',model.posts.filter(p=>p.status!=='published'&&new Date(when(p))>=now&&new Date(when(p))-now<7*86400000).length]].map(([a,b])=>`<div class="card"><span>${a}</span><b class="value medium">${b}</b></div>`).join('')}</div><div class="pub-toolbar"><div class="pub-tabs"><button data-pub-provider="all" class="${state.pubProvider==='all'?'active':''}">All</button>${providerKeys.map(p=>`<button data-pub-provider="${p}" class="${state.pubProvider===p?'active':''}">${providers[p].label}</button>`).join('')}</div><div class="pub-tabs"><button data-pub-status="all" class="${state.pubStatus==='all'?'active':''}">All</button>${['review','scheduled','draft','published'].map(s=>`<button data-pub-status="${s}" class="${state.pubStatus===s?'active':''}">${s==='review'?'Needs review':s[0].toUpperCase()+s.slice(1)}</button>`).join('')}</div><div class="searchrow"><input id="pubSearch" value="${esc(state.pubSearch)}" placeholder="Search queue…"></div></div>${sel?`<div class="workbench"><section class="queue"><div class="queue-head"><h2>Queue</h2><p>${list.length} visible items</p></div><div class="queue-list">${queue}</div></section><section class="preview"><div class="preview-head"><div><h2>${state.pubTab==='preview'?'Preview':state.pubTab==='copy'?'Destination copy':'Linked assets'}</h2><p>${providers[sel.provider].label} · revision ${sel.revision}</p></div><div class="preview-tabs">${['preview','copy','assets'].map(t=>`<button data-pub-tab="${t}" class="${state.pubTab===t?'active':''}">${t[0].toUpperCase()+t.slice(1)}</button>`).join('')}</div></div><div class="stage">${state.pubTab==='preview'?`<article class="social-card"><div class="social-head"><span class="social-avatar">IW</span><div><b>IntraWeb</b><p>${providers[sel.provider].label}</p></div></div><div class="copy">${esc(sel.copy)}</div>${mediaPreview(sel)}${sel.status==='published'?`<div class="engagement"><span>Views <b>${compactNum(sel.views)}</b></span><span>Reach <b>${compactNum(sel.reach)}</b></span><span>Likes <b>${compactNum(sel.likes)}</b></span><span>Shares <b>${compactNum(sel.shares)}</b></span></div>`:''}</article>`:state.pubTab==='copy'?`<div class="copy-editor"><h3>Destination copy</h3><p>This is the provider-specific version linked to the canonical content record.</p><textarea id="copyEdit">${esc(sel.copy)}</textarea><button class="btn primary" id="saveCopy">Save revision</button></div>`:`<div class="assets-grid"><div class="asset"><h3>${esc(sel.asset)}</h3><p>Linked media reference — binary is not stored in the CMS.</p></div><div class="asset"><h3>Source URL</h3><a href="${esc(sel.sourceUrl)}" target="_blank" rel="noopener">${esc(sel.sourceUrl)}</a></div><div class="asset"><h3>Format</h3><p>${esc(sel.format)} · ${esc(sel.duration)} · ${esc(sel.aspect)}</p></div><div class="asset"><h3>Revision ${sel.revision}</h3><p>Current destination-specific revision.</p></div></div>`}</div></section><aside class="inspector"><div class="inspector-head"><h2>Publication</h2><p>Destination controls</p></div><div class="inspector-body"><div class="field"><span>Date</span><input type="date" id="pubDate" value="${new Date(when(sel)).toISOString().slice(0,10)}"></div><div class="field"><span>Time</span><input type="time" id="pubTime" value="${new Date(when(sel)).toTimeString().slice(0,5)}"></div><div class="info"><div class="info-row"><span>Channel</span><b>${providers[sel.provider].label}</b></div><div class="info-row"><span>Theme</span><b>${esc(sel.theme)}</b></div><div class="info-row"><span>Format</span><b>${esc(sel.format)}</b></div><div class="info-row"><span>Revision</span><b>${sel.revision}</b></div><div class="info-row"><span>Status</span><b>${statusBadge(sel.status)}</b></div></div><div class="readiness"><h3>Readiness</h3><div>✓ Destination copy</div><div>✓ ${esc(sel.asset)}</div><div>${sel.status==='review'?'! Editorial approval required':'✓ Workflow status valid'}</div></div><div class="actions"><button class="btn primary" id="approve">Approve & schedule</button><div class="row"><button class="btn" id="revise">Request revision</button><button class="btn dark" id="publishNow">Publish now</button></div></div></div></aside></div>`:'<div class="panel"><p>No items match the current filters.</p></div>'}`;
}

function knowledge(){
  const q=state.knowledgeSearch.toLowerCase(),items=model.knowledge.filter(k=>(state.knowledgeKind==='all'||k.kind===state.knowledgeKind)&&(state.knowledgeTheme==='all'||k.theme===state.knowledgeTheme)&&(!q||`${k.title} ${k.source} ${k.summary}`.toLowerCase().includes(q)));
  const sources=[...new Map(model.knowledge.map(k=>[k.source,{source:k.source,items:0,delta:0}])).values()];model.knowledge.forEach(k=>{const s=sources.find(x=>x.source===k.source);s.items+=k.items;s.delta+=k.delta});sources.sort((a,b)=>b.items-a.items);
  return `${head('Knowledge Base','Catalog of everything the workspace knows: manual input, internal parsing and external intelligence with provenance and source dynamics.','<span class="pill accent">'+model.knowledge.reduce((a,k)=>a+k.items,0)+' items</span>')}<div class="filters"><label class="wide"><span>Search knowledge</span><input id="knowledgeSearch" value="${esc(state.knowledgeSearch)}" placeholder="Theme, source, fact, competitor…"></label><label><span>Source class</span><select id="knowledgeKind"><option value="all">All classes</option>${['Manual','Internal parse','External'].map(k=>`<option ${state.knowledgeKind===k?'selected':''}>${k}</option>`).join('')}</select></label><label><span>Theme</span><select id="knowledgeTheme"><option value="all">All themes</option>${themes.map(t=>`<option ${state.knowledgeTheme===t?'selected':''}>${t}</option>`).join('')}</select></label><button class="btn primary" id="addKnowledge">＋ Add knowledge</button></div><div class="knowledge-layout"><section class="knowledge-grid">${items.map(k=>`<article class="knowledge-item"><div class="meta"><span class="tag">${esc(k.kind)}</span><span class="pill">${esc(k.theme)}</span></div><h3>${esc(k.title)}</h3><p>${esc(k.summary)}</p><div class="knowledge-foot"><span>${esc(k.source)}</span><b>${k.items} items · ${k.delta>=0?'+':''}${k.delta} this week</b></div></article>`).join('')}</section><aside class="source-intel"><h2>Source intelligence</h2><p>Track leaders, competitors and external publishing dynamics over time.</p>${sources.map((s,i)=>`<div class="source-rank"><span class="rank">${i+1}</span><div><b>${esc(s.source)}</b><p>${s.delta>=0?'+':''}${s.delta} this week</p></div><strong>${s.items}</strong></div>`).join('')}</aside></div>`;
}

function hero(){return `${head('Hero Setup','Central workspace intelligence. Everything here informs research, creation and editorial decisions across the entire workspace.','<span class="pill accent">Workspace context</span>')}<div class="hero-grid"><section class="hero-card"><span class="eyebrow">BRAND & PRODUCT</span><h2>Core knowledge</h2><textarea id="heroProduct">${esc(model.hero.product)}</textarea></section><section class="hero-card"><span class="eyebrow">AUDIENCE</span><h2>Who we create for</h2><textarea id="heroAudience">${esc(model.hero.audience)}</textarea></section><section class="hero-card"><span class="eyebrow">VOICE</span><h2>Communication profile</h2><textarea id="heroVoice">${esc(model.hero.voice)}</textarea></section><section class="hero-card"><span class="eyebrow">LEARNING SOURCES</span><h2>Sources feeding Hero</h2><div class="source-list">${model.hero.sources.map((s,i)=>`<div class="source-row"><b>${esc(s)}</b><button class="icon-btn" data-remove-source="${i}" title="Remove">×</button></div>`).join('')}</div><div class="inline-add"><input id="heroSourceNew" placeholder="Website, channel, document, source…"><button class="btn" id="heroSourceAdd">Add</button></div></section></div><div class="hero-save"><button class="btn primary" id="saveHero">Save Hero Setup</button><span>Changes are used by all frontend modules in this preview.</span></div>`}

function settings(){return `${head('Settings','Workspace configuration, provider connections, dashboard composition, profile and Design System.','<span class="pill">Workspace settings</span>')}<div class="settings-grid"><section class="settings-card"><h2>Connections</h2><p>Provider accounts live here, not in Workspace navigation.</p>${providerKeys.map(p=>`<div class="connection-row"><span>${providerBadge(p)}</span><label class="switch"><input type="checkbox" data-connection="${p}" ${model.connections[p]?'checked':''}><span></span></label></div>`).join('')}</section><section class="settings-card"><h2>Dashboard configuration</h2><p>Dashboard is assembled from registered module widgets.</p>${[['pipeline','Publishing pipeline'],['coverage','Theme coverage'],['analytics','Analytics pulse'],['knowledge','Knowledge growth']].map(([k,l])=>`<div class="connection-row"><b>${l}</b><label class="switch"><input type="checkbox" data-widget="${k}" ${model.dashboardWidgets[k]?'checked':''}><span></span></label></div>`).join('')}</section><section class="settings-card"><h2>Profile</h2><div class="profile-settings"><span class="avatar large">N</span><div><b>Workspace administrator</b><p>IntraWeb 17 workspace</p></div></div><button class="btn">Profile settings</button></section><section class="settings-card"><span class="eyebrow">DESIGN SYSTEM</span><h2>Shared interface primitives</h2><p>Regular UI text is minimum 16pt. 14pt is reserved for tags, pills, badges and compact status elements. All modules use the same typography, fields, buttons, cards, provider identity and responsive rules.</p><div class="design-samples"><button class="btn primary">Primary action</button><button class="btn">Secondary</button>${providerBadge('youtube')}${statusBadge('scheduled')}<span class="pill">Pill · 14pt</span></div></section><section class="settings-card danger-zone"><h2>Preview data</h2><p>Reset the connected frontend demo to its original content and settings.</p><button class="btn" id="resetDemo">Reset demo data</button></section></div>`}

function openComposer(){
  const d=$('#composer');d.innerHTML=`<form class="composer-card"><div class="composer-head"><div><span class="eyebrow">CREATE</span><h2>New content</h2><p>Create one canonical idea and destination jobs for selected channels.</p></div><button type="button" class="icon-btn" id="closeComposer">×</button></div><div class="composer-grid"><label class="wide"><span>Title</span><input id="newTitle" required placeholder="Content title"></label><label><span>Theme</span><select id="newTheme">${themes.map(t=>`<option>${t}</option>`).join('')}</select></label><label><span>Format</span><select id="newFormat">${formats.map(f=>`<option>${f}</option>`).join('')}</select></label><label class="wide"><span>Copy / script</span><textarea id="newCopy" rows="6" placeholder="Canonical copy or script…"></textarea></label><label><span>Date</span><input id="newDate" type="date" value="${iso(1).slice(0,10)}"></label><label><span>Time</span><input id="newTime" type="time" value="10:00"></label><div class="wide"><span class="field-label">Destinations</span><div class="destination-grid">${providerKeys.map((p,i)=>`<label><input type="checkbox" name="dest" value="${p}" ${i<3?'checked':''}> ${providers[p].label}</label>`).join('')}</div></div></div><div class="composer-foot"><button type="button" class="btn" id="cancelComposer">Cancel</button><button type="submit" class="btn primary">Create destination jobs</button></div></form>`;d.showModal();$('#closeComposer').onclick=$('#cancelComposer').onclick=()=>d.close();$('form',d).onsubmit=e=>{e.preventDefault();const title=$('#newTitle').value.trim();if(!title)return;const dest=$$('input[name="dest"]:checked',d).map(x=>x.value);if(!dest.length){toast('Select at least one destination');return}const canonical=uid('canonical'),date=$('#newDate').value,time=$('#newTime').value;dest.forEach((p,i)=>model.posts.push({id:uid('post'),canonicalId:canonical,title,provider:p,status:'review',format:$('#newFormat').value,theme:$('#newTheme').value,copy:$('#newCopy').value||title,scheduledAt:new Date(`${date}T${time}:00`).toISOString(),duration:['Short','Reel'].includes($('#newFormat').value)?'00:45':$('#newFormat').value==='Long video'?'08:00':'—',aspect:['Short','Reel'].includes($('#newFormat').value)?'vertical':$('#newFormat').value==='Image post'?'square':$('#newFormat').value==='Long video'?'landscape':'text',revision:1,asset:'Linked asset pending',sourceUrl:`https://example.com/draft/${canonical}`,createdAt:new Date().toISOString(),views:0,reach:0,likes:0,comments:0,shares:0}));save();d.close();toast(`${dest.length} destination jobs created`);location.hash='#/publisher';setTimeout(render,0)}}

function addKnowledgeDialog(){const d=$('#composer');d.innerHTML=`<form class="composer-card"><div class="composer-head"><div><span class="eyebrow">KNOWLEDGE</span><h2>Add knowledge item</h2></div><button type="button" class="icon-btn" id="closeComposer">×</button></div><div class="composer-grid"><label class="wide"><span>Title</span><input id="kTitle" required></label><label><span>Source class</span><select id="kKind"><option>Manual</option><option>Internal parse</option><option>External</option></select></label><label><span>Theme</span><select id="kTheme">${themes.map(t=>`<option>${t}</option>`).join('')}</select></label><label class="wide"><span>Source</span><input id="kSource" placeholder="Source name"></label><label class="wide"><span>Summary</span><textarea id="kSummary" rows="5"></textarea></label></div><div class="composer-foot"><button type="button" class="btn" id="cancelComposer">Cancel</button><button type="submit" class="btn primary">Add knowledge</button></div></form>`;d.showModal();$('#closeComposer').onclick=$('#cancelComposer').onclick=()=>d.close();$('form',d).onsubmit=e=>{e.preventDefault();model.knowledge.unshift({id:uid('k'),kind:$('#kKind').value,title:$('#kTitle').value,theme:$('#kTheme').value,source:$('#kSource').value||'Manual input',items:1,delta:1,summary:$('#kSummary').value||'Manual workspace knowledge.'});save();d.close();render();toast('Knowledge item added')}}

function render(){state.route=location.hash.replace('#/','')||'dashboard';renderNav();const fn={dashboard,content:contentLibrary,analytics,calendar,publisher,knowledge,hero,settings}[state.route]||dashboard;$('#view').innerHTML=fn();bindScreen();bindRoutes();if(state.route==='analytics')bindChart()}
function bindScreen(){
  $$('[data-open-post]').forEach(b=>b.onclick=()=>goPublisher(b.dataset.openPost));
  $('#contentProvider')?.addEventListener('change',e=>{state.contentProvider=e.target.value;render()});$('#contentTheme')?.addEventListener('change',e=>{state.contentTheme=e.target.value;render()});$('#contentStatus')?.addEventListener('change',e=>{state.contentStatus=e.target.value;render()});$('#contentSearch')?.addEventListener('input',e=>{state.contentSearch=e.target.value;render()});
  $$('[data-metric]').forEach(b=>b.onclick=()=>{state.analyticsMetric=b.dataset.metric;render()});$('#aPeriod')?.addEventListener('change',e=>{state.analyticsPeriod=Number(e.target.value);render()});$('#aTheme')?.addEventListener('change',e=>{state.analyticsTheme=e.target.value;render()});$('#aFormat')?.addEventListener('change',e=>{state.analyticsFormat=e.target.value;render()});$$('[data-a-provider]').forEach(b=>b.onclick=()=>{const p=b.dataset.aProvider;if(p==='all'){state.analyticsProviders=new Set(providerKeys)}else if(state.analyticsProviders.size===providerKeys.length){state.analyticsProviders=new Set([p])}else if(state.analyticsProviders.has(p)){if(state.analyticsProviders.size>1)state.analyticsProviders.delete(p)}else state.analyticsProviders.add(p);render()});
  $$('[data-cal-view]').forEach(b=>b.onclick=()=>{state.calView=b.dataset.calView;render()});$$('[data-cal-provider]').forEach(b=>b.onclick=()=>{const p=b.dataset.calProvider;if(p==='all')state.calProviders.clear();else if(state.calProviders.has(p))state.calProviders.delete(p);else state.calProviders.add(p);render()});$('#calStatus')?.addEventListener('change',e=>{state.calStatus=e.target.value;render()});$$('[data-cal-move]').forEach(b=>b.onclick=()=>{const dir=Number(b.dataset.calMove);if(state.calView==='month')state.calCursor.setMonth(state.calCursor.getMonth()+dir);else if(state.calView==='week')state.calCursor.setDate(state.calCursor.getDate()+7*dir);else state.calCursor.setDate(state.calCursor.getDate()+dir);render()});$('[data-cal-today]')?.addEventListener('click',()=>{state.calCursor=new Date(now);render()});
  $$('[data-pub-provider]').forEach(b=>b.onclick=()=>{state.pubProvider=b.dataset.pubProvider;render()});$$('[data-pub-status]').forEach(b=>b.onclick=()=>{state.pubStatus=b.dataset.pubStatus;render()});$('#pubSearch')?.addEventListener('input',e=>{state.pubSearch=e.target.value;render()});$$('[data-select-post]').forEach(b=>b.onclick=()=>{state.pubSelected=b.dataset.selectPost;render()});$$('[data-pub-tab]').forEach(b=>b.onclick=()=>{state.pubTab=b.dataset.pubTab;render()});$('#saveCopy')?.addEventListener('click',()=>{const p=model.posts.find(x=>x.id===state.pubSelected);if(p){p.copy=$('#copyEdit').value;p.revision++;save();render();toast('Destination copy saved as new revision')}});$('#approve')?.addEventListener('click',()=>{const p=model.posts.find(x=>x.id===state.pubSelected);if(p){p.status='scheduled';applySchedule(p);save();render();toast('Approved and scheduled')}});$('#revise')?.addEventListener('click',()=>{const p=model.posts.find(x=>x.id===state.pubSelected);if(p){p.status='review';applySchedule(p);save();render();toast('Revision requested')}});$('#publishNow')?.addEventListener('click',()=>{const p=model.posts.find(x=>x.id===state.pubSelected);if(p){p.status='published';p.publishedAt=new Date().toISOString();p.views=p.views||0;p.reach=p.reach||0;save();render();toast('Marked published in preview')}});
  $('#knowledgeKind')?.addEventListener('change',e=>{state.knowledgeKind=e.target.value;render()});$('#knowledgeTheme')?.addEventListener('change',e=>{state.knowledgeTheme=e.target.value;render()});$('#knowledgeSearch')?.addEventListener('input',e=>{state.knowledgeSearch=e.target.value;render()});$('#addKnowledge')?.addEventListener('click',addKnowledgeDialog);
  $('#saveHero')?.addEventListener('click',()=>{model.hero.product=$('#heroProduct').value;model.hero.audience=$('#heroAudience').value;model.hero.voice=$('#heroVoice').value;save();toast('Hero Setup saved')});$('#heroSourceAdd')?.addEventListener('click',()=>{const v=$('#heroSourceNew').value.trim();if(v){model.hero.sources.push(v);save();render()}});$$('[data-remove-source]').forEach(b=>b.onclick=()=>{model.hero.sources.splice(Number(b.dataset.removeSource),1);save();render()});
  $$('[data-connection]').forEach(x=>x.onchange=()=>{model.connections[x.dataset.connection]=x.checked;save();toast('Connection setting updated')});$$('[data-widget]').forEach(x=>x.onchange=()=>{model.dashboardWidgets[x.dataset.widget]=x.checked;save();toast('Dashboard layout updated')});$('#resetDemo')?.addEventListener('click',()=>{localStorage.removeItem(STORE);model=loadModel();state.pubSelected=model.posts.find(p=>p.status==='review')?.id;render();toast('Demo data reset')});
}
function applySchedule(p){const d=$('#pubDate')?.value,t=$('#pubTime')?.value;if(d&&t){p.scheduledAt=new Date(`${d}T${t}:00`).toISOString();if(p.status!=='published')delete p.publishedAt}}

window.addEventListener('hashchange',render);renderShell();render();